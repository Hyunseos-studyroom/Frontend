//원시타입
let number = 1; //숫자형(number)
let str = "abc"; // 문자열형(string)
let bool = true; // shsflgud(boolen)
let undi = undefined; // undefined
let nul = null; // null(object)
let symbol = Symbol(); // symbol

console.log(typeof(Symbol()))